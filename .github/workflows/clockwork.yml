name: Clockwork

on:
  push:
    branches:
      - main
      - pre-releases
  pull_request:
    branches:
      - main

jobs:
  Compilation:
    uses: ./.github/workflows/compilation.yml
  Prerelease:
    needs: [Compilation]
    uses: ./.github/workflows/prerelease.yml
    secrets:
      token: ${{ secrets.ACCESS_TOKEN }}
    permissions:
      contents: write
